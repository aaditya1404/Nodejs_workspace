function greet(username) {
    console.log(username);
}

// This is the ES6 way of exporting the varibales or functions. 
// In react and every other similar stuff we use this only.
export default greet;